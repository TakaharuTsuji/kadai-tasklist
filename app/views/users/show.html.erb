    <div class="card">
      <div class="card-header">
        <h5 class="card-title"><%= @user.name %></h5>
      </div>
      <div class="card-body">
        <img class="rounded img-fluid" src="<%= gravatar_url(@user, { size: 100 }) %>" alt="">
      </div>
    </div>
    <div class="row">
      <a href="/" class='btn btn-lg btn-info mt-4 ml-4'>タスク一覧の表示へ</a>
    </div>

